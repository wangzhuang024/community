# ServerProperties
server.port=8080
server.servlet.context-path=/community

# ThymeleafProperties
spring.thymeleaf.cache=false

# DataSourceProperties
# 驱动
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 路径
spring.datasource.url=jdbc:mysql://localhost:3306/community?characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
# 账号密码
spring.datasource.username=root
spring.datasource.password=wangzhuang
# 连接池的配置
# 类型
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
# 最大连接数
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=5
# 超时时间
spring.datasource.hikari.idle-timeout=30000

# MybatisProperties
# 映射文件的配置位置
mybatis.mapper-locations=classpath:mapper/*.xml
# 实体类封装的包名
mybatis.type-aliases-package=com.nowcoder.community.entity
# 启动自动生成主键
mybatis.configuration.useGeneratedKeys=true
# 匹配 header_url <-> headerUrl
mybatis.configuration.mapUnderscoreToCamelCase=true


# 把com.nowcoder.community包下面的日志改成debug级别，在控制台打印更多的信息，便于调试
# logger
# logging.level.com.nowcoder.community=debug
# 打印日志文件
# logging.file=d:/newcoder/community.log

# MailProperties
spring.mail.host=smtp.qq.com
spring.mail.port=465
spring.mail.username=2268903339@qq.com
spring.mail.password=eurbdoeqefrnecaa
spring.mail.protocol=smtps
spring.mail.properties.mail.smtp.ssl.enable=true

# community  配置发邮件的域名
community.path.domain=http://localhost:8080